; -----------------------------------------
; 64-bit program that handles HTTP requests
; Author: Ian Goforth
; 
; Bind to IP and port given
; -----------------------------------------

section .text
        global _bind

_bind:
